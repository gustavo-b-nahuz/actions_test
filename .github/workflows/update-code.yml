name: Test Workflow

on:
    push:
        paths:
            - actions_test/folder/test.py

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Test Trigger
              run: echo "Workflow triggered successfully!"
